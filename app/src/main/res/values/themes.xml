<resources>
    <!-- Base application theme. -->
    <style name="Theme.Apod" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <!--<item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>-->
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/lobster_two</item>
        <item name="fontFamily">@font/lobster_two</item>
        <item name="android:textColor">#FFFFFF</item>

        <!--<item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorHint">#FFFFFF</item>-->

    </style>

    <style name="ThemeOverlay.NasaApis.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

    <style name="ButtonStyle"  parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/button_background</item>
        <item name="android:textColor">@color/gris_claro</item>
        <item name="paddingStart">6dp</item>
        <item name="android:paddingLeft">6dp</item>
        <item name="paddingEnd">6dp</item>
        <item name="android:paddingRight">6dp</item>
    </style>
</resources>